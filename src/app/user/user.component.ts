/**
 * @author ndkcha
 * @since 0.1.0
 * @version 0.1.0
 */

import { Component } from '@angular/core';

// before the update, it used to show the components of the directory
// so that i can get to know if i'm searching for file in right directory
// after the update i have to start typing the name of the file to find it.
// and search that accordingly for every directory hierarchy
import {  } from './../';

@Component({
    selector: "sm-main",
    templateUrl: "./user.component.html",
    styleUrls: ["./user.component.scss"]
})
export class UserComponent { }